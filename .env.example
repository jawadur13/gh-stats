# Copy to .env for local dev. NEVER commit a real token.
# Classic PAT with `repo` scope counts YOUR private commits.
# Others using your deployed link get public-only stats (GitHub privacy).
PAT_1=
CACHE_SECONDS=3600
PORT=3000
