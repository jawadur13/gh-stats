{
  "name": "gh-stats",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Own GitHub stats SVG service by jawadur13 - real data incl. private, reusable link",
  "scripts": {
    "dev": "tsx lib/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.3"
  },
  "engines": {
    "node": "22.x"
  }
}
